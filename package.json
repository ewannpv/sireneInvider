{
  "name": "sireneinvider",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "pm2:run": "pm2 start process.json",
    "pm2:stop": "pm2 stop all",
    "mongo:start": "systemctl start mongodb.service && exit 1",
    "mongo:stop": "systemctl stop mongodb.service && exit 1",
    "clean:data": "rm -rf src/data/worker && rm -rf src/data/json && exit 1",
    "lint": "eslint"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mongodb": "^4.2.0",
    "pm2": "^5.1.2"
  },
  "devDependencies": {
    "@types/node": "^16.11.1",
    "eslint": "^8.3.0"
  }
}
