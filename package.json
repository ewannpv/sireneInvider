{
  "name": "sireneinvider",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "mongo:start": "systemctl start mongodb.service && exit 0",
    "mongo:stop": "systemctl stop mongodb.service && exit 0",
    "clean:data": "rm -rf src/data/worker && rm -rf src/data/json && exit 0",
    "pm2:start": "npm run clean:data && pm2 start process.json",
    "pm2:stop": "pm2 stop all",
    "lint": "eslint"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mongodb": "^4.2.0",
    "pm2": "^5.1.2"
  },
  "devDependencies": {
    "@types/node": "^16.11.1",
    "eslint": "^8.3.0"
  }
}
